project.name = maps

base.dir = ../

build.dir = ${base.dir}/build
build.tempDir = ${build.dir}/${project.name}

build.jsPath = ${build.dir}/${project.name}/resources/js
build.cssPath = ${build.dir}/${project.name}/resources/css